node_modules/
dist/
.next/
.nextship/

docs/private

.env

# Generated at build time from packages/adapter, then shipped inside the CLI.
packages/cli/runtime/adapter.mjs

# The site installs its own dependencies; it is not part of the pnpm workspace.
site/node_modules
site/.next
site/next-env.d.ts

.claude/

# Copied from the repository root at pack time; the root copies are the source.
packages/cli/LICENSE
packages/cli/NOTICE
site/out
site/.wrangler
# A deploy of the streaming fixture records an app in the account it ran against.
conformance/streaming/app/nextship.json
